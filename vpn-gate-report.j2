{#
    This Jinaja2 template must receive the following variables:
    - `data`: A `VpnGateCsv` object with 'header' and 'rows' attributes.
    - `now`: The current timestamp for the footer.
#}

<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <meta name="description" content="VPN Gate CSV using Jinja2">
    <title>VPN Gate CSV</title>
    <style>
        body {
            font-family: Arial, sans-serif;
            line-height: 1.6;
            margin: 20px;
            color: #333;
        }
        table {
            width: 100%;
            border-collapse: collapse;
            margin: 20px 0;
        }
        th, td {
            border: 1px solid #ddd;
            padding: 8px;
            text-align: left;
        }
        th {
            background-color: #f2f2f2;
            font-weight: bold;
        }
        tr:nth-child(even) {
            background-color: #f9f9f9;
        }
    </style>
</head>
<body>
    <header>
        <h1>VPN Gate CSV</h1>
    </header>
    
    <main>
        <table>
            <thead>
                <tr>
                    {# Iterate over the 'header' attribute of the data object #}
                    {% for column in data.header %}
                    <th>{{ column }}</th>
                    {% endfor %}
                </tr>
            </thead>
            <tbody>
                {# Iterate over the 'rows' attribute of the data object #}
                {% for row in data.rows %}
                <tr>
                    {% for cell in row %}
                    <td>{{ cell }}</td>
                    {% endfor %}
                </tr>
                {% endfor %}
            </tbody>
        </table>
    </main>
    
    <footer>
        <p>Generated on {{ now.strftime('%Y-%m-%d %H:%M:%S') }}</p>
    </footer>
</body>
</html>